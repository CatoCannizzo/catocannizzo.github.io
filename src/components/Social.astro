---
interface Props {
  platform: string;
  username: string;
}

//The : Props below isn't necessary and I am not sure why.
const { platform, username }: Props = Astro.props;
---

<!-- !!! want to add fixed bottom-2  -->
<a
  class="rounded bg-primary p-1 text-primary-foreground hover:bg-primary/90"
  href={`https://www.${platform}.com/${username}`}>{platform}</a
>
